# Dependencies & build output (rebuilt inside the image)
node_modules
**/node_modules
dist
**/dist
.vite
coverage

# Secrets & local runtime data (never bake into the image)
.env
.env.*
!.env.example
*.db
*.db-journal
*.sqlite
*.sqlite3
*.session
session.txt
data

# VCS / tooling / docs not needed at runtime
.git
.gitignore
.gitattributes
*.log
npm-debug.log*
.DS_Store
.claude
Dockerfile
docker-compose.yml
.dockerignore
README.md
